[project]
name = "contoso-e2e"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "pytest>=9.1.1",
    "pytest-playwright>=0.7.1",
    "requests>=2.32.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
